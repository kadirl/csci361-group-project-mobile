import 'package:flutter/material.dart';

class ButtonSizes {
  // Extra small buttons (32px)
  static const xsHug = Size(0, 32);
  static const xsFill = Size.fromHeight(32);
  
  // Small buttons (40px)
  static const smHug = Size(0, 40);
  static const smFill = Size.fromHeight(40);
  
  // Medium buttons (56px)
  static const mdHug = Size(0, 56);
  static const mdFill = Size.fromHeight(56);
  
  // Large buttons (96px)
  static const lgHug = Size(0, 96);
  static const lgFill = Size.fromHeight(96);
  
  // Extra large buttons (136px)
  static const xlHug = Size(0, 136);
  static const xlFill = Size.fromHeight(136);
}