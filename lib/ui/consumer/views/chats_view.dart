import 'package:flutter/material.dart';

// Placeholder view for Consumer Chats
class ConsumerChatsView extends StatelessWidget {
  const ConsumerChatsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Consumer Chats'),
    );
  }
}

