import 'package:flutter/material.dart';

// Placeholder view for Consumer Dashboard/Home
class ConsumerDashboardView extends StatelessWidget {
  const ConsumerDashboardView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Consumer Dashboard'),
    );
  }
}

