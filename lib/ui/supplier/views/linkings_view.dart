import 'package:flutter/material.dart';

// Placeholder view for Supplier Linkings
class SupplierLinkingsView extends StatelessWidget {
  const SupplierLinkingsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Supplier Linkings'),
    );
  }
}


